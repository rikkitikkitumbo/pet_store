#index_title
  %h1 All Pets
  %p This will show ALL the pets

%div
  -@pet_stores.each do |pet_store|
    -pet_store.pets.where(query_params).each do |pet|
      .a_pet{:style => "background-color: #{pet.color}" }
        I am
        %strong= pet.name
        and I am a
        %strong= pet.color
        %strong= pet.species
        from
        %strong= PetStore.find(pet.pet_store_id).name
        !!

        %div
          %a
          =link_to 'Show', pet_store_pet_path(pet.pet_store_id, pet.id)
          =link_to 'Edit', edit_pet_store_pet_path(pet.pet_store_id, pet.id)
          =link_to "Delete", pet_store_pet_path(pet.pet_store_id, pet.id), method: :delete, confirm: "Are you sure you want to delete this pet?"